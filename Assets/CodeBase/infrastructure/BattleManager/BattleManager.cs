public class BattleManager {
    //Проверяет всех мобов и миников, достаточно ли они близки для атаки
    //Устанавливает target IHealth врага тому, кто может атаковать
    //Запоминает кто кого атакует
    //Подписывается на смерть IHealth
    //При смерти очищает target у атакующего и посылает запрос на удаление умершего
    //spawnerService.DeleteMobFromList(mob.Id); - spawnerService должен удалять gameobject умершего
    
    
}